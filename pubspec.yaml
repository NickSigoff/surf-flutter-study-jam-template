name: surf_practice_chat_flutter
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  surf_study_jam: ^0.2.0+1

  cupertino_icons: ^1.0.2

  flutter_bloc: ^8.0.1

  permission_handler: ^10.0.0
  geolocator: ^9.0.1
  google_maps_flutter: ^2.1.10

  shared_preferences: ^2.0.15

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.1
flutter:

  uses-material-design: true
